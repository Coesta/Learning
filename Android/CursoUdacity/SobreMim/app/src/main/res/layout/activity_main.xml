<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="meuNome"
            type="com.leocoesta.sobremim.MeuNome" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingStart="@dimen/padding"
        android:paddingEnd="@dimen/padding">

        <TextView
            android:id="@+id/nome_text"
            style="@style/nome_style"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@={meuNome.meuNome}"
            android:textAlignment="center" />

        <EditText
            android:id="@+id/apelido_edit"
            style="@style/nome_style"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ems="10"
            android:hint="@string/hint_informar_apelido"
            android:inputType="textPersonName|textImeMultiLine"
            android:textAlignment="center" />

        <Button
            android:id="@+id/enviar_button"
            style="@style/Widget.AppCompat.Button.Colored"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:text="@string/enviar_button" />

        <TextView
            android:id="@+id/apelido_text"
            style="@style/nome_style"
            android:text="@={meuNome.meuApelido}"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAlignment="center"
            android:visibility="gone" />

        <ImageView
            android:id="@+id/estrela_image"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/margem_layout"
            android:contentDescription="@string/estrela_amarela"
            app:srcCompat="@android:drawable/btn_star_big_on" />

        <ScrollView
            android:id="@+id/biografia_scroll"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <TextView
                android:id="@+id/biografia_text"
                style="@style/nome_style"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:lineSpacingMultiplier="@dimen/espacamento_linhas"
                android:text="@string/biografia" />
        </ScrollView>

    </LinearLayout>
</layout>
